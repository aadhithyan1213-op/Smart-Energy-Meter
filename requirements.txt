# ── Smart Energy Meter — Runtime Dependencies ──────────────────────────────
# Energy & IoT
paho-mqtt==1.6.1          # MQTT client (sensor data ingestion)
influxdb-client==1.38.0   # InfluxDB time-series storage

# API & Web
fastapi==0.111.0          # REST API framework
uvicorn[standard]==0.29.0 # ASGI server
pydantic==2.7.1           # data validation / schemas
httpx==0.27.0             # async HTTP client

# Data processing
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0

# Alerting & scheduling
apscheduler==3.10.4       # tariff / alert scheduler
python-dotenv==1.0.1      # env config

# Utilities
loguru==0.7.2
tenacity==8.3.0           # retry logic for IoT connections
